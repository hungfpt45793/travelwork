(function () {
    function t() {
        return navigator.getUserMedia = navigator.getUserMedia || navigator.webkitGetUserMedia || navigator.mozGetUserMedia || navigator.msGetUserMedia || null
    }

    function n() {
        if (t()) {
            var r = !1,
                f = {
                    video: !0,
                    audio: !1
                },
                n = null,
                i = document.getElementById("video"),
                e = navigator.getUserMedia(f, function (t) {
                    try {
                        i.srcObject = t
                    } catch (f) {
                        var u = window.URL || window.webkitURL;
                        i.src = u ? u.createObjectURL(t) : t
                    }
                    i.play();
                    r = !0;
                    n = t
                }, function (n) {
                    console.log("ERROR");
                    console.log(n)
                });
            document.getElementById("take-photo").addEventListener("click", function () {
                var n, t;
                r && (n = document.getElementById("imgdisplayHide"), n.width = i.videoWidth, n.height = i.videoHeight, n.getContext("2d").drawImage(i, 0, 0), t = n.toDataURL("image/jpeg"), document.getElementById("avatar-webcam").setAttribute("src", t), u());
                document.getElementById("cameraWebcam").setAttribute("value", t);
                jQuery("#camera-main").fadeOut()
            }, !1);
            document.getElementById("off-camera").addEventListener("click", function () {
                jQuery("#camera-main").hide();
                r && u()
            }, !1);

            function u() {
                i.pause();
                n.getAudioTracks().length && n.getAudioTracks()[0].stop && n.getAudioTracks().forEach(function (n) {
                    n.stop()
                });
                n.getVideoTracks().length && n.getVideoTracks()[0].stop && n.getVideoTracks().forEach(function (n) {
                    n.stop()
                })
            }
        } else console.log("KO")
    }

    document.getElementById("avatar-webcam").addEventListener("click", function () {
        /Android/i.test(navigator.userAgent) ? (navigator.userAgent.indexOf("Opera") || navigator.userAgent.indexOf("OPR")) != -1 ? jQuery("#fuAvatar").click() : navigator.userAgent.indexOf("Chrome") != -1 ? (jQuery("#camera-main").fadeIn(), n()) : navigator.userAgent.indexOf("Safari") != -1 ? jQuery("#fuAvatar").click() : navigator.userAgent.indexOf("Firefox") != -1 ? (jQuery("#camera-main").fadeIn(), n()) : navigator.userAgent.indexOf("MSIE") != -1 || !!document.documentMode == !0 ? jQuery("#fuAvatar").click() : jQuery("#fuAvatar").click() : /iPhone|iPad/i.test(navigator.userAgent) ? jQuery("#fuAvatar").click() : (navigator.userAgent.indexOf("Opera") || navigator.userAgent.indexOf("OPR")) != -1 ? jQuery("#fuAvatar").click() : navigator.userAgent.indexOf("Chrome") != -1 ? (jQuery("#camera-main").fadeIn(), n()) : navigator.userAgent.indexOf("Safari") != -1 ? jQuery("#fuAvatar").click() : navigator.userAgent.indexOf("Firefox") != -1 ? (jQuery("#camera-main").fadeIn(), n()) : navigator.userAgent.indexOf("MSIE") != -1 || !!document.documentMode == !0 ? jQuery("#fuAvatar").click() : jQuery("#fuAvatar").click()
    }, !1)
})();